version: "3.8"

services:

  web:
    build: ./web
    volumes:
      - image_dir:/images
    ports:
      - "8000:8000"

  automosaic:
    build: ./automosaic
    volumes:
      - image_dir:/images

  storage:
    build: ./storage
    volumes:
      - image_dir:/images

volumes:
  image_dir:
